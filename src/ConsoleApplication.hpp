#pragma once
#include "stdafx.hpp"

class ConsoleApplication {
    protected:
    STD map<String, String> parameters;
    public:
    
    ConsoleApplication(int argc, const char* argv[]) {
        String param_name
        for(size_t i=0;i<argc;i++) {
            const char* arg = argv[i];
            if(arg[0] == '-') {

            }
        }
    }
}